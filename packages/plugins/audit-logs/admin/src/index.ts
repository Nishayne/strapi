// Minimal admin entry point for audit-logs plugin
// This plugin is server-only and doesn't have an admin UI

export default {
  register() {
    // No admin UI registration needed
  },
  bootstrap() {
    // No admin UI bootstrap needed
  },
};
